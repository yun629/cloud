# DataCrunch Provider (Scaffolding)

This folder introduces the boilerplate required to onboard [DataCrunch](https://datacrunch.io/) into the Brev Cloud SDK.  It follo
ws the steps outlined in the **Add a Cloud Provider** manual and intentionally stops at typed stubs so that real API behavior can
 be filled in incrementally.
